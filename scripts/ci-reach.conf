# CI-reachability configuration (#lzcheckcireachguard), read by check-ci-reach.sh.
#
# workflow:  a workflow whose run: steps count as CI reach. Listing one here is a
#            claim that it runs on every push/PR — a tag-triggered release
#            workflow must not be listed, because reaching a gate only when you
#            cut a release is not reaching it.
# excuse:    a `make check` target that CI deliberately does not run, and why.
#            The target must be IN `check`'s closure: an excuse for a name make
#            never runs enforces nothing, so it is an error rather than a silent
#            no-op (#lzpinreachclosure). Which targets are in that closure is
#            itself pinned, by name, in EXPECTED_CLOSURE_TARGETS at the top of
#            check-ci-reach.sh — read that list to see what an excuse may name.
#
# The excuse list is the point of this file. It is the one place a reader can see
# what this binding does not enforce in CI, in the same spirit as KNOWN_UNCOVERED.
# Excuses are verified in both directions: an excuse for a target CI turns out to
# reach is a hard failure, so this list cannot rot into things that used to be
# true.

workflow: .github/workflows/ci.yml

# The other three workflows in this repo are deliberately NOT listed:
#
#   regressions.yml  schedule + workflow_dispatch only. It runs the loom model
#                    and the benchmark budgets, both of which `make check` also
#                    runs — but a nightly cron does not gate the commit that
#                    broke them, so counting it as reach would let a gate rot
#                    for a day at a time. Both gates are independently run by
#                    ci.yml, which is where their reach is claimed.
#   mdbook.yml       push to main only (no pull_request), and it builds and
#                    deploys the book. It runs no `make check` gate, so it has
#                    nothing to contribute either way.
#   release.yml      tag-triggered. Reaching a gate only when you cut a release
#                    is not reaching it.

# No excuses. Every gate `make check` runs is reached by the CI workflow above,
# which is a claim the guard re-proves on every run rather than a note.
