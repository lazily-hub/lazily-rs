[package]
name = "lazily"
version = "0.8.0"
edition = "2024"
description = "Lazy reactive signals with dependency tracking and cache invalidation"
license = "MIT"
repository = "https://github.com/btakita/lazily-rs"
keywords = ["reactive", "signals", "lazy", "dependency-tracking", "cache"]
categories = ["data-structures"]

[features]
async = ["dep:tokio"]
default = ["smallvec"]
distributed = []
instrumentation = []
loom = []
serde = ["dep:serde", "smallvec?/serde"]
std_sync_mutex = []
tokio = ["dep:tokio"]
vec_edges = []

[dependencies]
arc-swap = "1"
parking_lot = "0.12"
serde = { version = "1", optional = true, features = ["derive"] }
smallvec = { version = "1", optional = true }
tokio = { version = "1", optional = true, default-features = false, features = ["macros", "rt-multi-thread", "sync", "time"] }

[dev-dependencies]
criterion = "0.8"
loom = "0.7"
serde_json = "1"

[[example]]
name = "tokio_sync"
required-features = ["tokio"]

[[example]]
name = "instrumentation_profile"
required-features = ["instrumentation"]

[[bench]]
name = "context"
harness = false

[[bench]]
name = "profile"
harness = false
required-features = ["instrumentation"]

[[bench]]
name = "async_context"
harness = false
required-features = ["async"]

[[bench]]
name = "tokio_sync"
harness = false
required-features = ["tokio"]
